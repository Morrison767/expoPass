/**
 * ДИЗАЙН-ТОКЕНЫ QazExpoPass.
 *
 * Значения взяты из исходников ADATA (Desktop/adata-tnved), а не сняты
 * со скриншотов, поэтому совпадают точно. Там палитра описана в HSL;
 * здесь те же цвета в hex — рендерятся идентично.
 *
 * Три продуктовых ограничения ADATA соблюдаются и здесь:
 *   1. минимальный размер текста — 12px;
 *   2. кегли, отступы и высоты кратны 4px;
 *   3. бренд-цвет один — синий, второго акцента в системе нет.
 */

/* ─────────────────────────────────────────────────────────────
   1. БРЕНД — синий ADATA
   --primary: hsl(217 91% 55%) → #2474F5
   --accent-foreground: hsl(217 91% 45%) → #0A5ADB
   --accent (заливка активного пункта): hsl(214 95% 93%) → #DCEBFE
   ───────────────────────────────────────────────────────────── */
export const brand = {
  50: '#F0F6FF',
  100: '#DCEBFE', // заливка активного пункта меню
  200: '#BFDBFE',
  300: '#93C5FD',
  400: '#5A9BF8',
  500: '#3B82F6',
  600: '#2474F5', // ← основной бренд-цвет
  700: '#0A5ADB', // текст на светло-голубой заливке
  800: '#1D4ED8',
  900: '#1E3A8A',
  950: '#172554',
}

/* ─────────────────────────────────────────────────────────────
   2. НЕЙТРАЛЬНАЯ ШКАЛА
   --foreground: hsl(222 47% 11%) → #0F1729
   --muted-foreground: hsl(215 16% 47%) → #64758B
   --border / --input: hsl(214 20% 90%) → #E1E5EB
   --secondary: hsl(210 20% 96%) → #F3F5F7
   --muted: hsl(210 20% 94%) → #ECF0F3
   ───────────────────────────────────────────────────────────── */
export const ink = {
  25: '#FCFDFE',
  50: '#F8FAFC',
  100: '#F3F5F7', // secondary — утопленная поверхность
  200: '#ECF0F3', // muted — приглушённая заливка
  300: '#E1E5EB', // border / input
  400: '#CBD3DD',
  500: '#94A3B8',
  600: '#64758B', // muted-foreground — вторичный текст
  700: '#475569',
  800: '#293548',
  900: '#0F1729', // foreground — основной текст
  950: '#080E1A',
}

/* ─────────────────────────────────────────────────────────────
   3. ТЁМНЫЙ КОРПУС
   У ADATA тёмной темы нет — в globals.css описан только :root.
   Шкала оставлена, потому что переключатель в прототипе работает;
   оттенки подобраны под тот же синий, чтобы режим не выпадал.
   ───────────────────────────────────────────────────────────── */
export const slate = {
  950: '#070B12',
  900: '#0C121C',
  850: '#101725',
  800: '#141C2B',
  700: '#1C2637',
  600: '#263246',
  500: '#344258',
  400: '#59677E',
  300: '#7B899F',
  200: '#A2AEC1',
  100: '#C8D1DF',
  50: '#E7ECF4',
}

/* ─────────────────────────────────────────────────────────────
   4. СИГНАЛЬНЫЙ АКЦЕНТ — янтарный
   В палитре ADATA отдельного токена нет; взят из шкалы «индекса
   таможенной стоимости» (зелёный → жёлтый → оранжевый → красный).
   ───────────────────────────────────────────────────────────── */
export const signal = {
  50: '#FFFBEB',
  100: '#FEF3C7',
  200: '#FDE68A',
  300: '#FCD34D',
  400: '#FBBF24',
  500: '#F59E0B',
  600: '#D97706',
  700: '#B45309',
  800: '#92400E',
  900: '#78350F',
}

/**
 * ── 5. ПАЛИТРА СТАТУСОВ — 8 токенов ──
 *
 * Ключи сохранены с прошлых версий: на них завязаны статусы заявок
 * и учётных записей. `destructive` у ADATA — hsl(0 72% 51%) → #DC2828,
 * он и лёг в основу «conflict»; остальные подобраны в той же манере.
 *
 * Правило доступности прежнее: цвет никогда не единственный носитель
 * смысла — бейдж всегда = цвет + иконка-форма + текстовая метка.
 */
export const status = {
  /** Черновик — данные не отправлены */
  draft: {
    base: '#94A3B8',
    soft: '#F3F5F7',
    border: '#E1E5EB',
    text: '#475569',
    glow: 'transparent',
  },
  /** Ожидает решения */
  review: {
    base: '#F59E0B',
    soft: '#FFFBEB',
    border: '#FDE68A',
    text: '#B45309',
    glow: 'transparent',
  },
  /** Действует, подтверждено */
  confirmed: {
    base: '#16A34A',
    soft: '#F0FDF4',
    border: '#BBF7D0',
    text: '#15803D',
    glow: 'transparent',
  },
  /** Отклонено — на базе --destructive ADATA */
  conflict: {
    base: '#DC2828',
    soft: '#FEF2F2',
    border: '#FECACA',
    text: '#B91C1C',
    glow: 'transparent',
  },
  /** Согласовано, передано дальше — бренд-синий */
  paid: {
    base: '#2474F5',
    soft: '#F0F6FF',
    border: '#BFDBFE',
    text: '#1D4ED8',
    glow: 'transparent',
  },
  /** Возвращено на доработку */
  unpaid: {
    base: '#EA580C',
    soft: '#FFF7ED',
    border: '#FED7AA',
    text: '#C2410C',
    glow: 'transparent',
  },
  /** Завершено, истекло */
  done: {
    base: '#7C3AED',
    soft: '#F5F3FF',
    border: '#DDD6FE',
    text: '#6D28D9',
    glow: 'transparent',
  },
  /** Аннулировано */
  void: {
    base: '#BE123C',
    soft: '#FFF1F2',
    border: '#FECDD3',
    text: '#9F1239',
    glow: 'transparent',
  },
}

export type StatusToken = keyof typeof status

/** Деструктивные действия — --destructive ADATA */
export const danger = {
  50: '#FEF2F2',
  100: '#FEE2E2',
  200: '#FECACA',
  300: '#FCA5A5',
  400: '#F87171',
  500: '#EF4444',
  600: '#DC2828',
  700: '#B91C1C',
  800: '#991B1B',
  900: '#7F1D1D',
}

export const success = {
  50: '#F0FDF4',
  100: '#DCFCE7',
  200: '#BBF7D0',
  300: '#86EFAC',
  400: '#4ADE80',
  500: '#22C55E',
  600: '#16A34A',
  700: '#15803D',
  800: '#166534',
  900: '#14532D',
}

/**
 * ── 6. ТИПОГРАФИКА ──
 *
 * Open Sans — то же семейство, что в ADATA.
 *
 * Моноширинный берётся из системы, как и у ADATA: своего начертания
 * они не грузят. Нужен он только машинным идентификаторам — номерам
 * пропусков, кодам объектов, серийным номерам.
 */
export const fontFamily = {
  sans: [
    'var(--font-sans)',
    'system-ui',
    '-apple-system',
    '"Segoe UI"',
    'Roboto',
    'sans-serif',
  ],
  mono: ['ui-monospace', 'SFMono-Regular', 'Menlo', 'Consolas', 'monospace'],
}

/**
 * Тайп-скейл ADATA: строго на сетке 4px, минимум 12px.
 * Ступени 12 / 14 / 16 / 20 / 24 / 28 / 32 / 40 повторены один в один;
 * 48 и 56 добавлены для крупных чисел сводки.
 *
 * Рабочий кегль интерфейса — 14px, а не 16px. У ADATA text-sm (14px)
 * встречается 220 раз, text-xs (12px) — 155, а text-base (16px) всего
 * 42 раза и только в прозе. Поэтому `base` здесь равен 14px: на него
 * приходится основная масса разметки прототипа.
 *
 * Ниже 14px в системе одна ступень — 12px. Метка и подпись различаются
 * не размером, а начертанием и цветом: CAPS + semibold + приглушённый
 * тон против обычного начертания. Так же устроено и у ADATA.
 */
export const fontSize = {
  '2xs': ['0.75rem', { lineHeight: '1rem' }], //                                12/16 — CAPS-метки
  xs: ['0.75rem', { lineHeight: '1rem' }], //                                   12/16 — подписи, хинты
  sm: ['0.875rem', { lineHeight: '1.25rem' }], //                               14/20 — таблицы
  base: ['0.875rem', { lineHeight: '1.25rem' }], //                             14/20 — ОСНОВНОЙ размер
  md: ['1rem', { lineHeight: '1.5rem' }], //                                    16/24 — акцентный текст, проза
  lg: ['1.25rem', { lineHeight: '1.75rem' }], //                                20/28 — заголовок карточки
  xl: ['1.5rem', { lineHeight: '2rem' }], //                                    24/32 — заголовок панели
  '2xl': ['1.75rem', { lineHeight: '2.25rem' }], //                             28/36 — заголовок страницы
  '3xl': ['2rem', { lineHeight: '2.5rem' }], //                                 32/40
  '4xl': ['2.5rem', { lineHeight: '3rem' }], //                                 40/48
  '5xl': ['3rem', { lineHeight: '3.5rem' }], //                                 48/56 — числа сводки
  '6xl': ['3.5rem', { lineHeight: '4rem' }], //                                 56/64
}

export const fontWeight = { normal: '400', medium: '500', semibold: '600', bold: '700' }

/* ─────────────────────────────────────────────────────────────
   7. ОТСТУПЫ И РАЗМЕРЫ — сетка 4px
   Высоты контролов повторяют ADATA: h-8 / h-10 / h-11, шапка h-16.
   ───────────────────────────────────────────────────────────── */
export const spacing = {
  0.5: '0.125rem',
  1.5: '0.375rem',
  2.5: '0.625rem',
  3.5: '0.875rem',
  4.5: '1.125rem',
  5.5: '1.375rem',
  7.5: '1.875rem',
  13: '3.25rem',
  15: '3.75rem',
  17: '4.25rem',
  18: '4.5rem',
  22: '5.5rem',
  rail: '0.1875rem', // 3 — толщина статусной кромки
}

export const sizes = {
  'control-sm': '2rem', //           32 — h-8
  control: '2.5rem', //              40 — h-10, базовая высота контрола ADATA
  'control-lg': '2.75rem', //        44 — h-11
  topbar: '4rem', //                 64 — h-16, шапка ADATA
  row: '3rem', //                    48 — строка таблицы
  'row-lg': '3.5rem', //             56 — комфортная строка
  sidebar: '16rem', //              256
  'sidebar-collapsed': '4rem', //    64
  'panel-md': '30rem',
  'panel-lg': '40rem',
}

/* ─────────────────────────────────────────────────────────────
   8. РАДИУСЫ
   У ADATA --radius: 12px, а карточки скруглены на rounded-2xl (16px).
   Ступени подобраны так, чтобы существующая разметка прототипа
   попала в ту же геометрию без переписывания классов:
     rounded-sm  → мелкие чипы и иконки
     rounded     → кнопки и поля (12px, как rounded-xl у ADATA)
     rounded-md  → карточки (16px, как rounded-2xl)
     rounded-lg  → модальные окна
   ───────────────────────────────────────────────────────────── */
export const borderRadius = {
  none: '0',
  xs: '4px',
  sm: '8px',
  DEFAULT: '12px', // кнопки, поля, сегментированные переключатели
  md: '16px', //     карточки, таблицы, панели
  lg: '16px', //     модальные окна
  xl: '20px',
  '2xl': '24px',
  full: '9999px', // аватары, счётчики, бейджи-таблетки
}

export const zIndex = {
  base: '0',
  sticky: '30',
  dropdown: '40',
  overlay: '50',
  modal: '60',
  toast: '70',
}

/* ─────────────────────────────────────────────────────────────
   9. ДВИЖЕНИЕ
   У ADATA одна анимация: fade-in 0.22s ease-out. Держим тот же темп.
   ───────────────────────────────────────────────────────────── */
export const motion = {
  duration: {
    instant: '80ms',
    fast: '120ms',
    base: '220ms',
    slow: '280ms',
    slower: '360ms',
  },
  easing: {
    decelerate: 'cubic-bezier(0, 0, 0.2, 1)',
    spring: 'cubic-bezier(0.34, 1.3, 0.64, 1)',
    accelerate: 'cubic-bezier(0.4, 0, 1, 1)',
  },
}

/* Совместимость со старыми именами шкал */
export const navy = brand
export const obsidian = slate
export const beam = brand
